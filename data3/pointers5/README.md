# 1
* pointer constants and pointer variables
- an array is a constant 
- the array is the address where the system has chosen to place your array, and it will stay at this address until the program terminates
- You can’t say array++ any more than you can say 7++

# 2
- BUT while you can’t increment an address, you can increment a pointer that holds an address.
- This data shows how...

# 3
- notice how a pointer was assigned the value of the first element in the array. 
- now the pointer can be incremented with the '++' operator.