# 1
- this data shows the general way of declaring a struct and initializing the struct data inside a class constructor
- the struct is created in a separate file and included in the class file

# 2
- notice the different syntax of initializing struct data compared to the primitive data types.
-  the struct data is in format "structname{data, data}"
